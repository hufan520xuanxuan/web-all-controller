// 功能 Ins 取消关注设置详情界面
.unselectable
  .col-md-12
    .widget-container.fluid-height
      nothing-to-show(message='{{error}}', icon='fa-sitemap', ng-show='error')
      .content(ng-if='insAccount.account')
        label [取关配置清单]
      form.form
        .form-group
          label 开启状态
          input(bs-switch, type='checkbox', switch-size='small', switch-on-text='{{switchOnText}}',
          switch-off-text='{{switchOffText}}', ng-model='insAccount.config.follow.status', ng-true-value="1",
          ng-false-value="0", ng-change="switchChange()")
        .form-group
          span 操作间隔设置:
          input.form-control.sort-input(type='number')
          span 到
          input.form-control.sort-input(type='number')
          span.color-blue Tips:取消关注操作之间的间隔时间(范围取随机值)
        .form-group
          span 每次取消关注人数设置:
          input.form-control.sort-input(type='number')
          span 到
          input.form-control.sort-input(type='number')
          span.color-blue Tips:每次取消关注人数的区间范围(范围取随机值)
        .form-group
          span 设置执行功能时间段:
          input.form-control.sort-input(type='time')
          span 到
          input.form-control.sort-input(type='time')
          span.color-blue Tips:执行功能的时间段
          //多个checkbox不会写
        .form-group
          span 设置操作日期:
          label.checkbox-inline(ng-repeat='(key, day) in weekday')
            input(type='checkbox', ng-model='selectedWeek[key]', name='weekday', ng-value='{{day.value}}')
            span {{day.title}}

        .content
          label **筛选配置**
        .form-group
          label.checkbox
          input(type='checkbox', ng-model='notificationsEnabled', ng-click='enableNotifications()', checked)
          span(translate) 取关用户选项(二选一 所有/软件上关注的用户)
          //这里的输入框 不自动换行 怎么让他自己换行的
        .form-group
          label.checkbox
          input(type='checkbox', ng-model='notificationsEnabled', ng-click='enableNotifications()', checked)
          span 取关白名单列表设置:
            input.form-input(type='text')
          span.color-blue Tips:当前白名单中的用户不能被取消关注操作(每个词汇之间用逗号,隔开)
          //保存当前页面设置的按钮
        .form-btn
          label.btn
          span 保存当前配置




